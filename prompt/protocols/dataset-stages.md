# 数据使用阶段协议

2026-09-21 用户明确顺序：先在 78 对配对入门样例迭代稳定，再去完整本地真实+仿真质量集测试。当前阶段为 `starter78_development_only`。最新范围为 Zurich 363 对 + 已下载 PolyGNN mini 全部合格 199 对；用户确认本轮不扩展到 Munich 完整发布库。

## 当前允许

- 原始数据完整性/配对/固定质量检查、整理文件、确定划分、记录来源；不能把 QA 当重建实验。
- 使用 `../dataset/processed/complete_pairs_v001/manifests/starter78_development.json` 的固定 78 对做算法开发。它与前阶段同名清单哈希相同，指向原 `facade_pairs_v001` 文件，不改变该样例集。
- 入门 78 全部作为开发集，包括作者原 test 标签。真实 38 / 仿真 40 分开报告。
- 每次算法修改遵守原基准、上一版、当前版的冻结和横向渲染协议。主线仍从 v0.1.0 出发。
- 统一分析坐标入口现为 `../dataset/processed/standardized_pairs_v001/manifests/starter78.json`，保留同一78个建筑；完整562对标准化仅属数据准备。其他清单仍锁定。方法与源坐标恢复见 `docs/DATASET_METHODS_V001.md`。
- 首轮78小试已完成，迁移策略无收益且仍有失败/几何误差，阶段切换条件尚未成立。用户短暂提出的直接启用完整训练池已被后续78优先指令取代，期间没有运行432实验。

## 当前暂缓

- 完整项目训练 432 对中的新增 354 对（真实 274、仿真 80）不能提前进入修改反馈；封存测试 130 对（真实 51、仿真 79）不能提前重建或评分。
- 质量集合 562 对包含入门全部 78；真实 363 包含入门 38，仿真 199 包含入门 40。不能把入门数量再次相加。
- 统一 `all_train.json` 和分来源训练清单角色为 deferred_train，`all_test.json` 和分来源测试清单为 sealed_test；均 stage_locked。现有 `run_batch` 对角色及锁均校验，不能仅改 role 绕开阶段。

## 阶段切换必须留下的证据

用户已条件授权稳定后完整测试，不额外要求重复确认。条件尚未成立，不能仅因文件准备好就开测。

1. 在入门集上完成基准/候选复现与固定版本对比，完整保存成功、失败、退化和渲染，明确写出稳定性验收结论及其依据；尚未制定的数值门槛须在看封存成绩前固定。
2. 冻结算法代码、评测器、配置、数据/知识版本和清单哈希。
3. 在实验 summary 与此阶段记录登记切换，不覆盖当前冻结清单；为获准的新阶段生成新版本清单与独立评测入口。
4. 可以在完整训练池做后续训练/选择；测试 130 仅在最终候选冻结后用于声明的评测，不回流为自动演化反馈。真实/仿真分别统计，不跨坐标单位平均距离。多轮测试如发生须如实登记适应性使用。

## 测试解释边界

保留作者原划分：345 train / 18 test 的质量子集。18 个作者 test 全部在入门组。新项目测试的 51 栋源于作者 train，通过空间/形状组与入门和项目训练隔离，只能作为本项目内部未用于算法迭代的 Zurich 质量子集。

预训练 Point2Building 权重可能见过这些训练建筑；不能宣称对其是未见数据。近重复筛查与 BVH 不是穷尽证明，单城市筛选结果也不能支持跨城/全分布结论。

仿真作者标签保留为 100 train / 99 test，其中 20 个 test 已入门开发。项目训练因此为 120、测试为 79。按原作者测试候选剔除入门/精确重复/几何相似组，未根据算法分数选样；mini 缺源坐标，因此没有建立空间隔离证明。后续仍需审查各预训练比较方法的训练重叠。

## 2026-09-22 比赛4000推理授权

用户明确要求用initial v0.1.0对独立目录 `../dataset/testDataset/LiDAR_xyz` 的4000输入生成提交OBJ/ZIP。该授权仅适用于比赛4000的冻结基准推理和打包，不将完整配对训练432/封存130解锁。运行记录 `experiments/2026-09-22/initial-v010-test4000/summary.md`。不使用比赛GT，尚未上传或获得官方分数；先前development20已从该来源开发，后续成绩不能宣称对4000全部零开发接触。

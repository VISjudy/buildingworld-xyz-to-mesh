# v0.1.0-baseline · 初始简化基线（追溯用编号）

3 个历史样例；详见 reports 中的历史验证，不能视为官方评分。

- `../models/`：原坐标 OBJ 和结构化模型 JSON。
- `../renders/`：已有渲染、逐栋检查图；本次未重渲染。
- `../reports/`：验证、固定样本清单、历史交付记录。
- `../source/`：原始代码快照；当前可运行入口位于项目根目录。
- `../scenes/`：含点云的本地 Blender 场景，不上传。
- `../packages/models.zip`：只包含 OBJ 的下载包。
- `../index.html`：离线审查画廊。

本次只做归档和路径适配，没有重新生成历史模型。历史交付验证文件中的旧路径和哈希描述当时状态，不等于本次迁移检查。模型 JSON 的输入路径已改为相对项目根目录，OBJ 几何字节保持不变。历史说明中的旧链接仅供追溯，以本页路径为准。

没有真值网格与官方评测代码，因此 CD、ECD、NC、V_Ratio、F_Ratio 和 FINAL SCORE 未计算。代理门槛通过不代表重建完全正确。
